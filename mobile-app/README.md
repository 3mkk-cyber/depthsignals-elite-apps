# DepthSignals Elite Mobile App

Expo React Native starter for DepthSignals Elite mobile clients.

## Scripts

- `npm start` – start Metro bundler
- `npm run android` – run on Android
- `npm run ios` – run on iOS

Configure `apiBaseUrl` in `app.config.ts` for staging/production.
